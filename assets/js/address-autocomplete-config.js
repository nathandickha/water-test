// Address suggestions are disabled until the site owner supplies a browser-restricted
// Google Maps Platform key with the Places API enabled. When enabled, address text is
// sent to Google to provide suggestions. Restrict the key to this website's domains.
export const ADDRESS_AUTOCOMPLETE_CONFIG = Object.freeze({
  googleMapsApiKey: ''
});
